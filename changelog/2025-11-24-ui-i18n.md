# 2025-11-24 UI 与多语言优化

- 调整首页 `app/[locale]/page.tsx`：
  - 所有顶部徽章、信任文案、浮动卡片、页脚说明等改为通过 `HomePage` 文案键进行国际化。
  - 视觉保持简约，增强中英文显示一致性。
- 优化语言切换组件 `components/LanguageSwitcher.tsx`：
  - 增加了「语言 / Language」标签，使切换入口更清晰。
- 更新文案文件 `messages/zh.json` 与 `messages/en.json`：
  - 新增首页徽章、信任文案、ATS 评分、AI 优化状态、页脚说明等键值。
  - 为 Dashboard 增加 `settings` 文案键。
  - 新增 `Settings` 分组，作为设置页的标题与说明文案。
- 更新 Dashboard 页面 `app/[locale]/dashboard/page.tsx`：
  - 在右上角加入设置链接，指向 `/${locale}/settings`。
- 新增设置页 `app/[locale]/settings/page.tsx`：
  - 默认展示中文文案，同时支持切换中英文，聚焦于界面语言设置。
